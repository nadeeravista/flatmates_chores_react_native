export default ( state = 0, action) => {

    switch (action.type) {
        case "GET":
            return state
            break;
        default:
            return state
            break;
    }
}